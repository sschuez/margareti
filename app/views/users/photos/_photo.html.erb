<%= turbo_stream_from "#{dom_id(user)}_photostream" %>

<%= turbo_frame_tag dom_id(user, "photo"), class: "flex-column" do %>

  <div class="profile__photo">
    <%= photo_for(user) %>
  </div>

  <%= photo_edit_link_for(user) %>

<% end %>