<%= turbo_frame_tag nested_dom_id(user, block) do %>
  
  <%= simple_form_for [user, block] do |f| %>
    <%= form_error_notification(block) %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>

<% end %>